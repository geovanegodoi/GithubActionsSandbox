name: Workflow Sync Main

on:
  push:
    branches: 
      - main
    
  workflow_dispatch:

jobs:
  Syncronize:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v2
        with:
          fetch-depth: 0

      - name: Run a one-line script
        run: echo I am sync 'main' to 'develop'
